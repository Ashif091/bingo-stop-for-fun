import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Standard Next.js config without PWA plugin
  // PWA will work through manifest.json and manually added service worker
};

export default nextConfig;
